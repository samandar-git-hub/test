name: visual_buildify
description: "Visual Buildify - Mobil ilova interfeyslarini vizual tarzda yarating"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^17.3.0
  google_fonts: ^6.2.1
  uuid: ^4.5.1
  gap: ^3.0.1
  file_picker: ^11.0.2
  pasteboard: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
